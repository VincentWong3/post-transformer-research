# ViLBERT: Pretraining Task-Agnostic Visiolinguistic Representations for Vision-and-Language Tasks（2019）

**论文：** [arXiv](https://arxiv.org/abs/1908.02265) · Lu et al. · 2019

**核心思路：双流架构（视觉 Transformer + 文本 Transformer），通过 co-attention 交互。** 是早期的 V&L 预训练工作，和 LXMERT/UNITER 同属"encoder-only 多模态预训练"路线。

---

## 一、先搞清楚坑在哪

视觉和语言（Vision-and-Language, V&L）任务——比如看图回答问题（VQA）、根据描述找物体（Referring Expression）、找图配文字（Caption-based Image Retrieval）——都有一个共同需求：**把图像内容和自然语言对齐起来**，也就是做"视觉接地"（visual grounding）。

在 ViLBERT 之前，主流做法是各自为战：用 ImageNet 预训练的视觉模型提取特征，用 BERT/ELMo 之类的大语言模型编码文本，然后在具体任务上从头学怎么把两者对齐。听起来没啥毛病，但实际操作中问题很明显：

- 每个任务都单独学对齐，数据量有限时容易学到**偏执的接地**（myopic grounding）——比如 VQA 模型可能只是学到了语言先验（"红灯"后面常跟"停"），而不是真的看懂了图片 [Agrawal et al., CVPR 2018]。
- 不同任务之间没有共享的视觉-语言知识，模型在每项任务上从零学对齐，标注成本高、泛化差。

直观来说：你给一个人看大量图文配对的数据，他自然就能理解"狗配狗图、猫配猫图"。但 CV+NLP 各自预训练再拼起来的做法，相当于让两个人各学各的技能再临时配合，缺乏一个**统一的接地训练阶段**。

## 二、已有方法的真正问题

具体来说，之前的 V&L 方法在结构上有几个痛点：

1. **分阶段拼凑**：视觉特征由一个检测器（如 Faster R-CNN）提供，文本特征由 BERT 提供，两者之间只在最后的融合层交互。这意味着视觉和文本的 high-level 表示之间缺少**逐层交互**的机会——好比你让两个人分别读一本书的前半本和后半本，只在最后对一下笔记，而不是一边读一边讨论。

2. **单流架构的缺陷**：有些方法尝试把视觉和文本塞进同一个 Transformer 里（如 VideoBERT [Sun et al., 2019] 对视频做类似的事），也就是把图像区域做成离散的"视觉 token"和单词 token 一起送进 BERT。但这样做有几个硬伤：
   - 聚类（discretization）会丢掉视觉细节
   - 视觉和文本可能需要**不同深度的处理**——视觉特征是检测器已经提取好的 high-level 特征，不需要和文本一样从头学上下文
   - 强行把额外的大量"视觉 token"塞进预训练的 BERT 权重，可能会破坏已经学好的语言表示

这些问题的本质是：**视觉和文本在抽象层次和信息密度上天然不对称**，用一个统一的处理管道是粗暴的。

## 三、ViLBERT 的核心思路

ViLBERT 的核心洞察只有一句话：**让视觉和文本各走各的 Transformer 管道，只在必要的时候通过 co-attention 层交换信息。**

具体来说，ViLBERT 有两个并行的 BERT-style 流（visual stream + linguistic stream），每个流包含若干 Transformer block（TRM）和一种新的 **co-attentional transformer layer（Co-TRM）**。Co-TRM 层是信息交换的桥梁：视觉流产生的 key/value 传给文本流的 attention，文本流的 key/value 传给视觉流的 attention——两者互相"关注"对方，完成跨模态的上下文融合。

```
┌─────────────────────┐        ┌─────────────────────┐
│   Linguistic Stream  │        │    Visual Stream     │
│    (BERT-BASE)       │        │   (6 TRM blocks)     │
│                      │        │                      │
│   w0,w1,...,wT       │        │   v1,v2,...,vT       │
│        │             │        │        │             │
│     ┌──▼──┐          │        │    ┌──▼──┐          │
│     │ TRM │          │        │    │ TRM │          │
│     └──┬──┘          │        │    └──┬──┘          │
│        │             │        │       │             │
│     ┌──▼──┐          │◄──────►│   ┌──▼──┐          │
│     │Co-  │  K/V     │exchange│   │Co-  │          │
│     │TRM  │──────────┼────────┼───┤TRM  │          │
│     └──┬──┘          │  K/V   │   └──┬──┘          │
│        │             │        │       │             │
│     ┌──▼──┐          │        │    ┌──▼──┐          │
│     │ TRM │          │        │    │ TRM │          │
│     └──┬──┘          │        │    └──┬──┘          │
│        │             │        │       │             │
│   hw0,...,hwT        │        │   hv0,...,hvT       │
└─────────────────────┘        └─────────────────────┘
```

有意思的是，文本流的处理层数比视觉流多很多——在默认配置下，文本流用了 12 个 BERT 层（其中 6 个带 co-attention），而视觉流只有 6 个（全部带 co-attention）。这反映了设计直觉：视觉特征已经是检测器提取好的 high-level 表示，不需要大量上下文聚合；而文本需要从词序列中学习丰富的上下文语义。

预训练在两个代理任务（proxy tasks）上完成：
- **Masked Multi-modal Modeling**：随机遮住 15% 的文本词或图像区域，让模型恢复被遮住的部分
- **Multi-modal Alignment Prediction**：预测图文是否匹配

预训练数据集用的是 Conceptual Captions（约 310 万张带 alt-text 描述的网页图片），数据规模大、自动收集、无需人工标注。

预训练之后，对每个下游任务只需要加一个分类器做 fine-tuning，就能达到甚至超越当时的 SOTA。这在 2019 年是一个重要的范式转变——从"每个任务各自学接地"转变到"预训练接地能力，再迁移到下游任务"。

## 四、方法细节

### 4.1 双流架构设计

两个流各自处理自己的模态，但结构和参数不共享：

- **Textual stream**：初始化自 BERT$_{\text{BASE}}$（12 层 Transformer，hidden size 768，12 头 attention，论文第 3.1 节）。输入是 BPE 分词的 token 序列 + 位置编码 + segment 编码。
- **Visual stream**：6 个 Transformer block（hidden size 1024，8 头 attention）。输入是从 Faster R-CNN（ResNet-101 骨干，Visual Genome 预训练）提取的区域特征，每个区域 $v_i$ 是 ROI pooling 后的 1024 维平均池化特征。由于图像区域没有天然顺序，额外编码 5 维空间位置向量（归一化左上/右下坐标 + 面积占比），投影到和视觉特征同维度后相加。

**Co-Attention 层的工作流程**：

标准的 Transformer self-attention 计算：给定当前层的表示 $H^{(l)}$，生成 Q、K、V 三个矩阵，然后用 Q 和 K 做点积 attention，加权聚合 V。

Co-TRM 把"self"换成了"cross"：视觉流的 Q 还是用自己的，但 K 和 V 来自文本流（反过来也一样——文本流的 Q 用自己的，K/V 来自视觉流）。这样视觉 attention 看到的不再是"自己区域的 attention"，而是"文本驱动的视觉 attention"——图像区域根据当前文本的语义被加权。反过来，文本流也得到了视觉引导的文本 attention。这模仿了 V&L 模型中常见的视觉-文本交叉 attention 机制 [Anderson et al., CVPR 2018]。

### 4.2 预训练任务

**任务 1：Masked Multi-modal Modeling（MMM）**

和 BERT 的 Masked Language Model (MLM) 对称——随机遮挡 15% 的输入 token（含文本词和图像区域），让模型从剩余信息中重建被遮住的部分。

- **文本侧**：和 BERT 完全一样（80% 替换为 [MASK]，10% 随机替换，10% 不变）
- **图像侧**：90% 将被遮挡区域的视觉特征全置零，10% 保持不变。但注意——重建目标不是直接回归被遮住的特征值，而是预测该区域的语义类别分布。这个类别分布来自同一个 Faster R-CNN 检测器输出的 softmax 类别概率。模型通过最小化 KL 散度来学习。

为什么这样设计？直觉是：语言往往只描述 high-level 的视觉语义（"狗"、"红色"），不可能精确重建 1024 维的特征向量。用检测器输出的语义类别分布作为监督信号更合理、也更温和。

**任务 2：Multi-modal Alignment Prediction（MAP）**

类似 BERT 的 Next Sentence Prediction (NSP)——给定一对（图像，文本），判断它们是否匹配（即文本是否描述了这幅图像）。

模型取 $\mathbf{h}_{IMG}$（视觉流第一个位置 [IMG] token 的输出）和 $\mathbf{h}_{CLS}$（文本流 [CLS] token 的输出），计算逐元素乘积作为联合表示，然后通过一个线性二分类层输出匹配概率。由于 Conceptual Captions 中的图文天然配对，负样本通过随机替换图像或文本来生成。

两个预训练任务的损失等权重求和。

### 4.3 实现细节

- 语言流初始化自 BERT$_{\text{BASE}}$（BookCorpus + English Wikipedia 预训练）
- 视觉特征提取：Faster R-CNN + ResNet-101（Visual Genome 预训练），每张图保留 10~36 个检测框
- 训练：8 块 TitanX GPU，batch size 512，训练 10 个 epoch
- 优化器：Adam，初始学习率 $1\times 10^{-4}$，带 warm-up 的线性衰减
- 两个训练任务损失等权

## 五、公式详解

### 5.1 Co-Attention 变换公式

给定视觉表示 $\mathbf{H}_V^{(l)}$ 和文本表示 $\mathbf{H}_W^{(l)}$，标准的 self-attention 先做投影：

$$\mathbf{Q}_V = \mathbf{H}_V^{(l)} \mathbf{W}_Q^V, \quad \mathbf{K}_V = \mathbf{H}_V^{(l)} \mathbf{W}_K^V, \quad \mathbf{V}_V = \mathbf{H}_V^{(l)} \mathbf{W}_V^V$$

$$\mathbf{Q}_W = \mathbf{H}_W^{(l)} \mathbf{W}_Q^W, \quad \mathbf{K}_W = \mathbf{H}_W^{(l)} \mathbf{W}_K^W, \quad \mathbf{V}_W = \mathbf{H}_W^{(l)} \mathbf{W}_V^W$$

Co-TRM 的做法是将 key/value 对换：

$$\text{Attention}_{V\rightarrow W} = \text{Softmax}\left(\frac{\mathbf{Q}_V \mathbf{K}_W^\top}{\sqrt{d_k}}\right) \mathbf{V}_W$$

$$\text{Attention}_{W\rightarrow V} = \text{Softmax}\left(\frac{\mathbf{Q}_W \mathbf{K}_V^\top}{\sqrt{d_k}}\right) \mathbf{V}_V$$

其中 $\sqrt{d_k}$ 是缩放因子（$d_k = \text{hidden\_size} / \text{num\_heads}$）。第一行表示"视觉的 query 关注文本的 key/value"——文本指导视觉的 attention pooling；第二行相反，"文本的 query 关注视觉的 key/value"——视觉指导文本的 attention pooling。

直觉理解：**视觉流学到的是"这段文本最关注哪些图像区域"；文本流学到的是"这幅图像最应该关注哪些词"**。

### 5.2 MMM 的 KL 散度损失

对每个被遮住的图像区域 $i$，模型输出的类别分布记为 $p_i$（由 $\mathbf{h}_{v_i}$ 过一个线性层 + softmax 得到），检测器对该区域输出的类别分布记为 $q_i$（softmax 后的语义类别，来自 Visual Genome 预训练的 Faster R-CNN）：

$$\mathcal{L}_{\text{mask}} = \sum_{i \in \mathcal{M}_V} \text{KL}(q_i \| p_i) = \sum_{i \in \mathcal{M}_V} \sum_{c=1}^{C} q_{i,c} \log \frac{q_{i,c}}{p_{i,c}}$$

其中 $\mathcal{M}_V$ 是被遮住的图像区域集合，$C$ 是检测器的类别数。文本侧的 MLM 损失和标准 BERT 相同：对每个被遮住的词 $j$，用交叉熵最大化正确词的概率。

### 5.3 MAP 的二分类损失

$$\mathbf{h}_{\text{joint}} = \mathbf{h}_{IMG} \odot \mathbf{h}_{CLS}$$

$$p(\text{aligned}) = \sigma(\mathbf{w}^\top \mathbf{h}_{\text{joint}} + b)$$

其中 $\odot$ 是逐元素乘积（element-wise product），$\sigma$ 是 sigmoid 函数。损失是标准的二分类交叉熵：

$$\mathcal{L}_{\text{align}} = -y \log p(\text{aligned}) - (1 - y) \log (1 - p(\text{aligned}))$$

$y=1$ 时图文匹配，$y=0$ 时不匹配。

## 六、实验结果

ViLBERT 在 4 个下游任务上都超越了当时的 SOTA：

| 任务 | 指标 | 之前 SOTA | ViLBERT† (no pretrain) | ViLBERT (full) |
|------|------|-----------|------------------------|---------------|
| VQA 2.0 | test-dev | 70.34 (DFAF) | 68.93 | **70.55** (70.92 test-std) |
| VCR Q→A | test-dev | 65.1 (R2C) | 69.26 | **72.42** (73.3 test-std) |
| VCR QA→R | test-dev | 67.3 (R2C) | 71.01 | **74.47** (74.6 test-std) |
| VCR Q→AR | test-dev | - | 49.48 | **54.04** (54.8 test-std) |
| RefCOCO+ val | accuracy | 65.33 (MAttNet) | 68.61 | **72.34** |
| RefCOCO+ testA | accuracy | 71.62 | 75.97 | **78.52** |
| RefCOCO+ testB | accuracy | 56.02 | 58.44 | **62.61** |
| Flickr30k R@1 | retrieval | 48.60 (SCAN) | 45.50 | **58.20** |
| Flickr30k R@5 | retrieval | 77.70 | 76.78 | **84.90** |
| Flickr30k R@10 | retrieval | 85.20 | 85.02 | **91.52** |

关键发现：

1. **双流架构优于单流**：在不做预训练的条件下对比（ViLBERT† 68.93 vs Single-Stream† 65.90），双流在 VQA test-dev 上高出约 3 个点——证明了分模态处理本身的价值，和预训练的效果是叠加的。
2. **预训练带来显著提升**：ViLBERT（full）比 ViLBERT†（不做预训练）在 VCR Q→AR 上提升了 4.6 个点，在 RefCOCO+ val 上提升约 3.7 个点。这个 gap 验证了预训练任务的必要性。
3. **超越任务定制 SOTA**：VCR Q→A 上 ViLBERT (72.42) 比 R2C (65.1) 高出 7 个点，RefCOCO+ val 上比 MAttNet (65.33) 高出 7 个点，**说明通用预训练模型可以胜过精心定制的任务专用模型**。

**消融实验：视觉流深度。** 将 Co-TRM→TRM 重复块的数量从 2 层增加到 8 层，发现 VQA 和图像检索随深度单调提升（到 6 层最优），而 VCR 和 RefCOCO+ 对深度不那么敏感甚至更浅好——说明不同任务需要的视觉上下文聚合程度不同。

**消融实验：预训练数据量。** 取 Conceptual Captions 的 25%、50%、100% 做预训练，性能随数据量单调提升（论文 Table 3）。零样本图像检索的 R@1 从 0%（0% 数据）→ 20.40%（25%）→ 26.76%（50%）→ 31.86%（100%），**暗示更大的预训练数据还能继续涨点**。

**零样本检索（Zero-shot Image Retrieval）**：直接用预训练后的 MAP 打分做检索（不做任何 fine-tuning），Flickr30k R@1 达 31.86%，而之前 SOTA SCAN 是 48.60%（但 SCAN 是 full fine-tune 的结果）。考虑到是 zero-shot，这个结果表明 ViLBERT 确实学到了有意义的视觉-语言对齐。

## 七、位置

### 前驱工作

- **BERT** [Devlin et al., 2019]：ViLBERT 的文本流直接继承 BERT$_{\text{BASE}}$ 的架构和权重，MLM 和 NSP 两个预训练任务也被改造为多模态版本
- **Bottom-Up and Top-Down Attention** [Anderson et al., CVPR 2018]：视觉特征的提取方式和"文本引导的视觉 attention"的设计思路直接继承自此工作
- **Conceptual Captions** [Sharma et al., ACL 2018]：为 V&L 预训练提供了大规模弱监督数据（310 万自动收集的图文对），是 ViLBERT 能成功预训练的前提条件

### 同期竞品

- **VideoBERT** [Sun et al., 2019]：最早将 BERT 扩展到视频-语言联合建模的工作，但它用的是**单流架构**（visual token 和 text token 接入同一套 Transformer），且聚焦于烹饪视频而非自然图像。ViLBERT 通过双流 co-attention 证明了单流不是最优解。
- **LXMERT** [Tan & Bansal, 2019]：和 ViLBERT 几乎同期（arXiv 仅差 11 天），同样用 Transformer encoder 做 V&L 预训练，但 LXMERT 用了三个预训练目标（MLM、VLM、VQA）和跨模态 encoder 而非 co-attention。
- **UNITER** [Chen et al., 2019]：晚两个月，用单流架构但在预训练任务设计上更全面（MLM + MRM + ITM + WRA），用条件掩码策略解决了单流架构中视觉 token 数量不固定的问题。

### 后续影响

ViLBERT 开启了 V&L 预训练的浪潮，此后几乎所有 V&L encoder-only 模型都直接或间接受其影响：

- **ViLT** [Kim et al., 2021]：把视觉特征的提取方式从 Faster R-CNN 简化为 linear projection，大幅降低预计算成本，但维度核心理念（双流/单流之争）源自 ViLBERT 等先驱的工作。
- **ALBEF** [Li et al., 2021]：引入了 momentum distillation 和 contrastive learning，用单流架构做图文对齐（ITC + MLM + ITM），是 ViLBERT 之后的一个重要演进。
- **VLMO** [Bao et al., 2022]：提出了一个三合一的架构（single-stream + dual-stream 的融合方案），既享受单流的深度融合，又保留双流的高效编码——可以说是对 ViLBERT 双流 vs LXMERT/UNITER 单流争论的一种统一。
- **BEiT-3** [Wang et al., 2022]：进一步将视觉和文本统一为"多模态 token"做 multi-way attention，把"双流还是单流"的问题推向更高层级的抽象。

*后续影响列表基于已有知识整理，建议通过 Semantic Scholar / Google Scholar 验证引用最多的 follow-up works。*

## 八、局限

1. **视觉特征依赖外部检测器**：ViLBERT 的视觉输入来自 Faster R-CNN 的 region proposals，这是预计算好的、不可微的。这意味着：
   - 每张图需要额外跑一遍物体检测，推理速度慢
   - 无法 end-to-end 联合优化视觉特征提取和下游任务
   - 检测器只提供 region-level 特征，丢失了全局场景信息
   - **改进方向**：后来 ViLT 解决了这个问题（linear projection 替代 R-CNN），但泛化能力有损

2. **不支持文本生成**：ViLBERT 是 encoder-only 架构，只能做分类/匹配/排序类任务，无法做 captioning（生成文本）。且由于 BERT 是双向模型，传统的 beam search 解码不适用。论文提到的"生成 caption"只能用 MCMC 采样 [Wang & Cho, 2019] 这种低效方式。

3. **Conceptual Captions 数据质量有限**：数据是自动从网页 alt-text 爬取并清洗的，噪声较大，有些 caption 带倾向性（如"a massive display of fireworks that will fill the grounds"——典型的 editorialized caption），不够"干净"。

4. **消融实验略显单薄**：论文只在 VQA 和 RefCOCO+ 两个任务上做了预训练消融（无对齐损失、无掩码损失等），覆盖不完整。零样本检索虽有意义但仍远低于 fine-tune 后的 SCAN，说明预训练学到的接地能力仍有较大提升空间。

## 九、小结

ViLBERT 是 V&L 预训练领域的开创性工作之一。**核心贡献**是提出了一种双流 co-attention Transformer 架构，让视觉和文本分别用独立的处理管道编码、只在必要层交换信息。**关键洞察**是视觉和文本在抽象层次上不对称，分开处理再交互比硬塞进统一管道要好。

通过在 Conceptual Captions 上做两个简单的预训练任务（masked modeling + alignment prediction），ViLBERT 学到的视觉-语言接地能力可以在 4 个完全不同的下游任务上 fine-tune 后超越当时的 task-specific SOTA。这标志着 V&L 领域从"任务内学接地"到"预训练接地再迁移"的范式转变——此后 LXMERT、UNITER、ALBEF、BEiT-3 等模型都沿着这条路径发展，形成了 V&L 预训练的大趋势。

---
*Paper reading generated by paper-read skill. Rounds: 0. Accuracy: - Logic: - Readability: - Markdown: -*
